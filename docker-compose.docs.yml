services:
  docs:
    image: breemind_back_local_docs
    container_name: breemind_back_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./breemind_back:/app/breemind_back:z
    ports:
      - '9000:9000'
    command: /start-docs
